<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
  <title>BizHub - Marketplace</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300;400;600;700&family=Lora:ital,wght@0,400;0,600;0,700;1,400;1,600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
  
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --primary:      #ffbe33;
    --primary-dark: #e0a820;
    --dark:         #222831;
    --dark2:        #1a1f27;
    --white:        #ffffff;
    --light-bg:     #f8f8f8;
    --text-muted:   #666666;
    --border:       #ebebeb;
  }
  html { scroll-behavior: smooth; }
  body { font-family: 'Lora', serif; background: var(--white); color: var(--dark); overflow-x: hidden; }

  /* PAGE HERO BANNER */
  .page-hero {
    height: 280px; position: relative; display: flex; align-items: center;
    padding-top: 72px; overflow: hidden;
  }
  .page-hero-bg { position: absolute; inset: 0; background: url('images/hero-bg.png') center/cover no-repeat; opacity: 0.45; }
  .page-hero-overlay { position: absolute; inset: 0; background: linear-gradient(135deg, rgba(26,31,39,0.92) 0%, rgba(34,40,49,0.75) 100%); }
  .page-hero-content { position: relative; z-index: 2; padding: 0 5%; }
  .page-hero-eyebrow { font-family: 'Josefin Sans', sans-serif; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.22em; text-transform: uppercase; color: var(--primary); display: flex; align-items: center; gap: 10px; margin-bottom: 0.8rem; }
  .page-hero-eyebrow::before { content: ''; width: 24px; height: 1px; background: var(--primary); }
  .page-hero h1 { font-family: 'Lora', serif; font-size: clamp(2rem, 4vw, 3.2rem); font-weight: 700; font-style: italic; color: var(--white); line-height: 1.15; letter-spacing: 0.01em; }
  .page-hero h1 span { color: var(--primary); }

  /* SECTION */
  .section { padding: 80px 5%; }
  .section-alt { background: var(--light-bg); }
  .section-dark { background: var(--dark); }
  .section-header { text-align: center; margin-bottom: 3rem; }
  .section-label { font-family: 'Josefin Sans', sans-serif; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.22em; text-transform: uppercase; color: var(--primary); display: block; margin-bottom: 0.6rem; }
  .section-header h2 { font-family: 'Lora', serif; font-size: clamp(1.6rem, 3vw, 2.4rem); font-weight: 700; color: var(--dark); margin-bottom: 0.6rem; }
  .section-header.dark h2 { color: var(--white); }
  .section-header p { font-size: 0.95rem; color: var(--text-muted); max-width: 520px; margin: 0 auto; line-height: 1.7; }

  /* BUTTONS */
  .btn-yellow { background: var(--primary); color: var(--dark); padding: 13px 30px; border-radius: 8px; font-family: 'Josefin Sans', sans-serif; font-weight: 700; font-size: 0.85rem; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; display: inline-block; border: none; cursor: pointer; transition: background 0.2s, transform 0.15s; }
  .btn-yellow:hover { background: var(--primary-dark); transform: translateY(-2px); }
  .btn-outline-dark { background: transparent; color: var(--dark); border: 1.5px solid var(--dark); padding: 12px 28px; border-radius: 8px; font-family: 'Josefin Sans', sans-serif; font-weight: 700; font-size: 0.85rem; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; display: inline-block; transition: background 0.2s, color 0.2s; }
  .btn-outline-dark:hover { background: var(--dark); color: var(--white); }

  /* CARDS */
  .card-grid { display: grid; gap: 1.5rem; }
  .card-grid-3 { grid-template-columns: repeat(3,1fr); }
  .card-grid-2 { grid-template-columns: repeat(2,1fr); }
  .biz-card { background: var(--white); border-radius: 12px; overflow: hidden; box-shadow: 0 4px 20px rgba(0,0,0,0.07); border: 1.5px solid transparent; transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; }
  .biz-card:hover { transform: translateY(-5px); box-shadow: 0 14px 40px rgba(0,0,0,0.11); border-color: rgba(255,190,51,0.3); }
  .biz-card-img { width: 100%; height: 200px; object-fit: cover; display: block; }
  .biz-card-body { padding: 24px; }
  .biz-card-tag { display: inline-block; background: rgba(255,190,51,0.13); color: var(--primary-dark); font-family: 'Josefin Sans', sans-serif; font-size: 0.68rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; padding: 3px 10px; border-radius: 100px; margin-bottom: 10px; }
  .biz-card-title { font-family: 'Lora', serif; font-size: 1.05rem; font-weight: 700; color: var(--dark); margin-bottom: 8px; }
  .biz-card-text { font-size: 0.86rem; color: var(--text-muted); line-height: 1.65; margin-bottom: 16px; }
  .biz-card-footer { display: flex; justify-content: space-between; align-items: center; padding-top: 14px; border-top: 1px solid var(--border); }
  .biz-card-price { font-family: 'Josefin Sans', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--dark); }
  .biz-card-link { font-family: 'Josefin Sans', sans-serif; font-size: 0.78rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--primary); text-decoration: none; display: flex; align-items: center; gap: 6px; }
  .biz-card-link:hover { color: var(--primary-dark); }

  /* FOOTER */
  footer { background: var(--dark2); color: var(--white); padding: 60px 5% 28px; }
  .footer-grid { display: grid; grid-template-columns: 1.8fr 1fr 1fr 1fr; gap: 3rem; padding-bottom: 3rem; border-bottom: 1px solid rgba(255,255,255,0.07); }
  .footer-brand-name { font-size: 1.3rem; font-weight: 700; font-family: 'Josefin Sans', sans-serif; letter-spacing: 0.1em; text-transform: uppercase; color: var(--white); display: inline-block; margin-bottom: 0.8rem; text-decoration: none; }
  .footer-brand-name span { color: var(--primary); }
  .footer-desc { font-size: 0.84rem; font-family: 'Lora', serif; color: rgba(255,255,255,0.4); line-height: 1.7; max-width: 210px; margin-bottom: 1.2rem; }
  .footer-socials { display: flex; gap: 8px; }
  .social-icon { width: 36px; height: 36px; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.09); border-radius: 8px; display: flex; align-items: center; justify-content: center; color: rgba(255,255,255,0.5); font-size: 0.85rem; text-decoration: none; transition: background 0.2s, color 0.2s, border-color 0.2s; }
  .social-icon:hover { background: var(--primary); color: var(--dark); border-color: var(--primary); }
  .footer-col h4 { font-size: 0.78rem; font-weight: 700; font-family: 'Josefin Sans', sans-serif; letter-spacing: 0.14em; text-transform: uppercase; color: var(--white); margin-bottom: 1.2rem; }
  .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 0.55rem; }
  .footer-col ul a { font-size: 0.85rem; font-family: 'Lora', serif; color: rgba(255,255,255,0.4); text-decoration: none; transition: color 0.2s; }
  .footer-col ul a:hover { color: var(--primary); }
  .footer-bottom { padding-top: 1.5rem; display: flex; justify-content: space-between; align-items: center; font-size: 0.78rem; color: rgba(255,255,255,0.25); flex-wrap: wrap; gap: 0.5rem; }

  /* FADE UP */
  .fade-up { opacity: 0; transform: translateY(28px); transition: opacity 0.65s ease, transform 0.65s ease; }
  .fade-up.visible { opacity: 1; transform: translateY(0); }
  .fade-up.d1 { transition-delay: 0.1s; } .fade-up.d2 { transition-delay: 0.2s; }

  /* FILTERS */
  .filters-bar { display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 2.5rem; }
  .filter-btn { font-family: 'Josefin Sans', sans-serif; font-size: 0.75rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; padding: 8px 20px; border-radius: 100px; border: 1.5px solid var(--border); background: var(--white); color: var(--text-muted); cursor: pointer; transition: all 0.2s; }
  .filter-btn.active, .filter-btn:hover { background: var(--primary); color: var(--dark); border-color: var(--primary); }

  /* PROGRESS BAR */
  .progress-bar-wrap { height: 8px; background: var(--border); border-radius: 100px; overflow: hidden; margin: 10px 0; }
  .progress-bar-fill { height: 100%; border-radius: 100px; background: linear-gradient(90deg, var(--primary), var(--primary-dark)); }

  /* FORUM CARD */
  .forum-card { background: var(--white); border-radius: 12px; overflow: hidden; box-shadow: 0 4px 20px rgba(0,0,0,0.07); border: 1.5px solid transparent; transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; }
  .forum-card:hover { transform: translateY(-4px); box-shadow: 0 14px 36px rgba(0,0,0,0.1); border-color: rgba(255,190,51,0.25); }
  .forum-card-header { padding: 22px 24px; color: var(--white); display: flex; align-items: center; gap: 12px; }
  .forum-card-header i { font-size: 1.4rem; opacity: 0.9; }
  .forum-card-header h5 { font-family: 'Lora', serif; font-size: 1rem; font-weight: 700; font-style: italic; margin: 0; }
  .forum-card-body { padding: 20px 24px; }
  .forum-card-body p { font-size: 0.86rem; color: var(--text-muted); line-height: 1.65; margin-bottom: 16px; }
  .forum-meta { display: flex; justify-content: space-between; align-items: center; padding-top: 14px; border-top: 1px solid var(--border); font-size: 0.78rem; color: var(--text-muted); }
  .forum-meta span { display: flex; align-items: center; gap: 5px; }
  .forum-join { font-family: 'Josefin Sans', sans-serif; font-size: 0.75rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--primary); text-decoration: none; display: block; text-align: center; margin-top: 14px; padding: 9px; border: 1.5px solid rgba(255,190,51,0.35); border-radius: 6px; transition: background 0.2s, color 0.2s; }
  .forum-join:hover { background: var(--primary); color: var(--dark); }

  /* TIMELINE / ACTIVITY */
  .activity-item { display: flex; gap: 16px; padding: 16px 0; border-bottom: 1px solid var(--border); align-items: flex-start; }
  .activity-item:last-child { border-bottom: none; }
  .activity-dot { width: 10px; height: 10px; border-radius: 50%; background: var(--primary); margin-top: 6px; flex-shrink: 0; }
  .activity-content { flex: 1; }
  .activity-title { font-size: 0.9rem; font-weight: 600; color: var(--dark); margin-bottom: 3px; }
  .activity-title a { color: var(--primary); text-decoration: none; }
  .activity-title a:hover { text-decoration: underline; }
  .activity-time { font-size: 0.75rem; color: var(--text-muted); font-family: 'Josefin Sans', sans-serif; letter-spacing: 0.04em; }

  /* PROFILE */
  .profile-avatar { width: 110px; height: 110px; border-radius: 50%; object-fit: cover; border: 3px solid var(--primary); }
  .stat-pill { background: var(--light-bg); border: 1px solid var(--border); border-radius: 10px; padding: 16px 20px; text-align: center; }
  .stat-pill-num { font-family: 'Josefin Sans', sans-serif; font-size: 1.6rem; font-weight: 700; color: var(--dark); display: block; }
  .stat-pill-label { font-size: 0.72rem; color: var(--text-muted); letter-spacing: 0.05em; margin-top: 3px; }

  /* DASHBOARD */
  .dash-stat { background: var(--white); border-radius: 12px; padding: 24px; border: 1.5px solid var(--border); display: flex; gap: 16px; align-items: center; transition: border-color 0.2s; }
  .dash-stat:hover { border-color: var(--primary); }
  .dash-stat-icon { width: 52px; height: 52px; border-radius: 10px; background: rgba(255,190,51,0.12); display: flex; align-items: center; justify-content: center; font-size: 1.3rem; color: var(--primary); flex-shrink: 0; }
  .dash-stat-num { font-family: 'Josefin Sans', sans-serif; font-size: 1.8rem; font-weight: 700; color: var(--dark); line-height: 1; }
  .dash-stat-label { font-size: 0.78rem; color: var(--text-muted); margin-top: 3px; }
  .dash-grid { display: grid; grid-template-columns: 2fr 1fr; gap: 1.5rem; }
  .dash-panel { background: var(--white); border-radius: 12px; padding: 28px; border: 1.5px solid var(--border); }
  .dash-panel h5 { font-family: 'Lora', serif; font-size: 1.05rem; font-weight: 700; color: var(--dark); margin-bottom: 1.2rem; padding-bottom: 0.8rem; border-bottom: 1px solid var(--border); }
  .quick-link { display: flex; align-items: center; gap: 12px; padding: 12px 0; border-bottom: 1px solid var(--border); text-decoration: none; color: var(--dark); font-size: 0.88rem; font-weight: 500; transition: color 0.2s; }
  .quick-link:last-child { border-bottom: none; }
  .quick-link:hover { color: var(--primary); }
  .quick-link-icon { width: 32px; height: 32px; background: rgba(255,190,51,0.1); border-radius: 6px; display: flex; align-items: center; justify-content: center; font-size: 0.85rem; color: var(--primary); flex-shrink: 0; }

  /* FORM */
  .biz-form { background: var(--light-bg); border-radius: 16px; padding: 40px; border: 1.5px solid var(--border); }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
  .form-group { margin-bottom: 1.1rem; }
  .form-group label { display: block; font-family: 'Josefin Sans', sans-serif; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--dark); margin-bottom: 6px; }
  .form-group input, .form-group select, .form-group textarea { width: 100%; background: var(--white); border: 1.5px solid var(--border); border-radius: 8px; padding: 12px 16px; font-family: 'Lora', serif; font-size: 0.88rem; color: var(--dark); outline: none; transition: border-color 0.2s; appearance: none; }
  .form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--primary); }
  .form-group textarea { resize: vertical; min-height: 110px; }
  .form-submit { width: 100%; background: var(--primary); color: var(--dark); border: none; padding: 15px; border-radius: 8px; font-family: 'Josefin Sans', sans-serif; font-size: 0.9rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; cursor: pointer; transition: background 0.2s, transform 0.15s; display: flex; align-items: center; justify-content: center; gap: 8px; }
  .form-submit:hover { background: var(--primary-dark); transform: translateY(-1px); }

  /* RESPONSIVE */
  @media (max-width: 1024px) { .card-grid-3 { grid-template-columns: 1fr 1fr; } .footer-grid { grid-template-columns: 1fr 1fr; } .dash-grid { grid-template-columns: 1fr; } }
  @media (max-width: 768px) { .nav-links { display: none; } .hamburger { display: flex; } .card-grid-3, .card-grid-2 { grid-template-columns: 1fr; } .footer-grid { grid-template-columns: 1fr 1fr; } .form-row { grid-template-columns: 1fr; } }
  @media (max-width: 480px) { .footer-grid { grid-template-columns: 1fr; } }
</style>
</head>
<body>
<div class="page-hero">
  <div class="page-hero-bg"></div>
  <div class="page-hero-overlay"></div>
  <div class="page-hero-content">
    <div class="page-hero-eyebrow">Produits & Services</div>
    <h1><em>Marketplace</em> BizHub</h1>
  </div>
</div>
<section class="section section-alt">
  <div class="section-header fade-up">
    <span class="section-label">Catalogue</span>
    <h2>Produits & Services disponibles</h2>
    <p>Explorez notre sélection de solutions business professionnelles.</p>
  </div>
  <div style="display:flex;gap:0.5rem;flex-wrap:wrap;margin-bottom:2.5rem;justify-content:center;" class="fade-up">
    <button class="filter-btn active" onclick="filterProducts('all',this)">Tout</button>
    <button class="filter-btn" onclick="filterProducts('software',this)">Software</button>
    <button class="filter-btn" onclick="filterProducts('consulting',this)">Consulting</button>
    <button class="filter-btn" onclick="filterProducts('design',this)">Design</button>
    <button class="filter-btn" onclick="filterProducts('tools',this)">Tools</button>
  </div>
  <div class="card-grid card-grid-3" id="productGrid">
<div class="biz-card fade-up" data-cat="software">
  <img src="images/f1.jpg" alt="Project Management Suite" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Software</div>
    <div class="biz-card-title">Project Management Suite</div>
    <p class="biz-card-text">Suivi de projets complet, collaboration équipe et gestion de timeline pour les équipes agiles.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$99/mo</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="consulting">
  <img src="images/f2.jpeg" alt="Business Strategy Consulting" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Consulting</div>
    <div class="biz-card-title">Business Strategy Consulting</div>
    <p class="biz-card-text">Conseils d'experts sur l'expansion marché et les stratégies de croissance.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$150/hr</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="design">
  <img src="images/f3.jpeg" alt="Brand Identity Design" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Design</div>
    <div class="biz-card-title">Brand Identity Design</div>
    <p class="biz-card-text">Logo, charte graphique et identité visuelle pour créer une première impression mémorable.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$750</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="tools">
  <img src="images/f4.jpeg" alt="API Integration Tools" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Tools</div>
    <div class="biz-card-title">API Integration Tools</div>
    <p class="biz-card-text">Connecteurs API personnalisés pour automatiser vos workflows et intégrer vos outils.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$299</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="software">
  <img src="images/f5.jpeg" alt="Analytics Dashboard" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Software</div>
    <div class="biz-card-title">Analytics Dashboard</div>
    <p class="biz-card-text">Intelligence business en temps réel. Suivez vos KPIs et prenez des décisions data-driven.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$79/mo</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="design">
  <img src="images/f6.jpeg" alt="Website Development" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Design</div>
    <div class="biz-card-title">Website Development</div>
    <p class="biz-card-text">Sites web responsive avec design moderne et optimisation SEO pour votre présence en ligne.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$1 200</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="consulting">
  <img src="images/f7.jpeg" alt="Marketing Strategy" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Consulting</div>
    <div class="biz-card-title">Marketing Strategy</div>
    <p class="biz-card-text">Plans de marketing digital complets et stratégies go-to-market pour booster votre croissance.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$120/hr</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="tools">
  <img src="images/f8.jpeg" alt="Email Campaign Manager" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Tools</div>
    <div class="biz-card-title">Email Campaign Manager</div>
    <p class="biz-card-text">Automatisation d'emailing professionnel avec templates et analytics pour nurture vos leads.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$49/mo</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div>
<div class="biz-card fade-up" data-cat="software">
  <img src="images/f9.jpeg" alt="Cloud Storage Solution" class="biz-card-img">
  <div class="biz-card-body">
    <div class="biz-card-tag">Software</div>
    <div class="biz-card-title">Cloud Storage Solution</div>
    <p class="biz-card-text">Stockage cloud sécurisé et scalable avec collaboration d'équipe et contrôle total des données.</p>
    <div class="biz-card-footer">
      <div class="biz-card-price">$59/mo</div>
      <a href="#" class="biz-card-link">Commander <i class="fa fa-arrow-right"></i></a>
    </div>
  </div>
</div></div>
  <div style="text-align:center;margin-top:3rem;" class="fade-up">
    <a href="#" class="btn-yellow">Explorer plus de produits</a>
  </div>
</section>
<script>
function filterProducts(cat, btn) {
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  document.querySelectorAll('#productGrid .biz-card').forEach(c => {
    c.style.display = (cat === 'all' || c.dataset.cat === cat) ? '' : 'none';
  });
}
</script>

<footer>
  <div class="footer-grid">
    <div>
      <a href="{{ path('app_index') }}" {# TODO: route not found #} class="footer-brand-name">Biz<span>Hub</span></a>
      <p class="footer-desc">Connecter entreprises, investisseurs, étudiants et créateurs pour collaborer, apprendre et grandir ensemble.</p>
      <div class="footer-socials">
        <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-twitter"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-linkedin-in"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
      </div>
    </div>
    <div class="footer-col">
      <h4>Plateforme</h4>
      <ul>
        <li><a href="{{ path('app_dashboard') }}" {# TODO: route not found #}>Dashboard</a></li>
        <li><a href="{{ path('app_users_reviews') }}" {# TODO: route not found #}>Profil</a></li>
        <li><a href="{{ path('app_market_products') }}" {# TODO: route not found #}>Marketplace</a></li>
        <li><a href="{{ path('app_forums_events') }}" {# TODO: route not found #}>Forums</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Business</h4>
      <ul>
        <li><a href="{{ path('app_donations_investments') }}" {# TODO: route not found #}>Investissement</a></li>
        <li><a href="{{ path('app_elearning_incubation') }}" {# TODO: route not found #}>Formations</a></li>
        <li><a href="{{ path('app_elearning_incubation') }}" {# TODO: route not found #}>Incubation</a></li>
        <li><a href="#">Partenaires</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Légal</h4>
      <ul>
        <li><a href="#">Confidentialité</a></li>
        <li><a href="#">CGU</a></li>
        <li><a href="#">Cookies</a></li>
        <li><a href="#">À propos</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span>© <span id="displayYear"></span> BizHub Platform. Tous droits réservés.</span>
    <span>Fait avec ♥ pour les entrepreneurs</span>
  </div>
</footer>
<script>
  document.getElementById('displayYear').textContent = new Date().getFullYear();
  document.getElementById('hamburger').addEventListener('click', () => document.getElementById('mobileNav').classList.add('open'));
  document.getElementById('mobileClose').addEventListener('click', () => document.getElementById('mobileNav').classList.remove('open'));
  document.querySelectorAll('.mobile-nav a').forEach(a => a.addEventListener('click', () => document.getElementById('mobileNav').classList.remove('open')));
  window.addEventListener('scroll', () => document.getElementById('mainNav').classList.toggle('scrolled', window.scrollY > 50));
  const obs = new IntersectionObserver(entries => { entries.forEach(e => { if(e.isIntersecting){ e.target.classList.add('visible'); obs.unobserve(e.target); } }); }, {threshold:0.1});
  document.querySelectorAll('.fade-up').forEach(el => obs.observe(el));
</script></body></html>